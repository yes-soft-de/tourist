export interface SidebarMenuItemsChildren {
    title?: string;                     // [optional]: optional if translate is used
    link?: string;                       // [optional]: item link
    translate?: string;                 // [optional] : use with multi language
    value?: string;                     // [optional] : use to add action to the item
}