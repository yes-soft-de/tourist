##
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tourist-webserver
  namespace: tourist
  labels:
    app: apache
spec:
  replicas: 1
  selector:
    matchLabels:
      app: apache
  template:
    metadata:
      labels:
        app: apache
    spec:
      containers:
      - name: php-apache
        image: gcr.io/yescloud/galaxyback
        ports:
        - containerPort: 80
        volumeMounts:
        - name: tourist-pvc-sym-m
          mountPath: /var/www/upload/image
      volumes:
      - name: tourist-pvc-sym-m
        persistentVolumeClaim:
          claimName: tourist-pvc-sym-uploud
---
apiVersion: v1
kind: Service
metadata:
  name: tourist-web-service
  namespace: tourist
  labels:
    run: web-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    protocol: TCP
  selector:
    app: apache
---
apiVersion: v1                  
kind: PersistentVolumeClaim     
metadata:                       
   name: tourist-pvc-sym-uploud
   namespace: tourist
spec:                           
   accessModes:                  
   - ReadWriteOnce              
   resources:                   
      requests:                 
         storage: 1Gi   
